{
  "name": "sim_transport",
  "version": "1.0.0",
  "description": "",
  "main": "main.ts",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commonjs": "0.0.1",
    "rxjs": "^5.5.5"
  },
  "scripts": {
    "browserify": "browserify main.js -o bundle.js -s app",
    "start": "(tsc || true) && browserify main.js -o bundle.js -s app"
  },
  "devDependencies": {
    "typescript": "^2.6.2"
  }
}